# invalid configuration: shows the parser rejecting a malformed file
# run: ./webserv configs/invalid.conf  ->  exits with an error message

server {
	listen	127.0.0.1:9999;
	location / {
		root	www;
	}
